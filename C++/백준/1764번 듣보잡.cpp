#include <iostream>
#include <map>
#include <string>
#include <unordered_set>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	unordered_set<string> a;
	unordered_set<string> b;

	int n, m;
	string input;

	cin >> n >> m;

	for(int i = 0; i < n + m; i++)
	{
		cin >> input;
	}
}